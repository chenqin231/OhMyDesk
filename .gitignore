# Rust
/target
**/target
Cargo.lock.bak

# ts-rs：#[ts(export)] 的默认产物（冗余，真实类型导出到 src/admin-web/src/lib/types/）
src/protocol/bindings/

# Node / 前端构建产物
node_modules/
dist/
.vite/
*.tsbuildinfo

# 环境与密钥（数据不出网，严禁入库）
.env
.env.*
*.key
*.pem
credentials.*

# 杂项
*.log
.DS_Store

# 发布产物（dist/linux dist/windows dist/macos，cargo/packaging 脚本生成）
/dist/

# SQLite 数据文件（运行时生成）
ohmydesk.db
ohmydesk.db-*
*.db
*.db-wal
*.db-shm

# 开发对话记录（不开源）
chat/
chat*.txt

# v0 设计稿原型（已提炼到 src/admin-web，原型归档在 proto/ 不开源）
proto/
