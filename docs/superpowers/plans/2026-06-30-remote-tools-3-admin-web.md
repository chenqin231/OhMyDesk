# 远程工具增强 ③：admin-web 管理端 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` to execute this plan. 逐步派发任务给 subagent，每步独立验证后再进入下一步；可测逻辑步骤先写测试再实现（TDD），UI 步骤完成后人工验证。每个步骤结束后回到主上下文做检查点（lint/类型检查/构建），不要把多个步骤合并成一次大改。

## Goal

为 OhMyDesk 管理端（`src/admin-web`）落地设计 spec `docs/superpowers/specs/2026-06-30-remote-command-file-chat-design.md` 中的「计划③」范围：

1. **增强现有命令/文件能力**：`FilePanel` 文件传输从纯文字提示升级为 per-file 百分比**进度条**；`CommandPanel` 输入框支持 **↑↓ 历史命令回溯**。
2. **新增「会话消息」第 4 标签**：`remote-session.tsx` 增第 4 标签；`remote-tools.tsx` 新增 `ChatPanel`；`store.ts` 增 `sendChat` action + `chatMessages` state + 下行 `chat_message` 处理。
3. **懒推流**：admin 切标签时，离开/进入「远程控制」标签分别发 `set_capture{active:false/true}`，与 Slint 端一致，省内网带宽。
4. **审计页扩展**：`lib/adapters/audit.ts` 的 `summarize` 扩展识别 `command`/`file_transfer`/`chat`；`audit-timeline-sheet.tsx` 的 `KIND_ICON` 补 `chat` 图标；`audit-cells.tsx` 补对应文案/图标导出。

## Architecture

- **数据单向流（项目规范 A 组）**：所有服务端状态只存在于 zustand `store.ts`，组件用 `useStore` 选择器订阅，不自持服务端状态。本计划新增的 `chatMessages` / `fileProgress` 均落在 store；组件只持有纯本地 UI 状态（输入框文本、命令历史游标）。
- **WS 收发**：组件 → `store.sendEnvelope(payload: Message)` → `transport.send(Envelope)`；下行 → `store.initTransport` 的 `onEnvelope` 分发器按 `env.payload.type` 增分支。
- **跨端类型唯一来源**：所有协议类型只引用 `src/admin-web/src/lib/types/`（ts-rs 生成物）。`chat_message`（`Message`）、`set_capture`（`Message`）、`chat`（`AuditType`）由**计划①（协议+服务端）**改 `src/protocol` 并 `cargo test export_all` 自动重导出到该目录——本计划**禁止手改生成物**，仅消费。
- **测试**：admin-web 当前无测试框架（`package.json` 仅 `dev`/`build`/`preview`，无 vitest/jest，无 `lint`）。本计划**前置引入 vitest 最小配置**（与既有 Vite + TS 基建零冲突），对纯逻辑（chat 下行 reducer、fileProgress 累计、audit `summarize`、命令历史游标）做 TDD；UI 组件用 `pnpm -C src/admin-web dev` 人工验证。全程以 `pnpm -C src/admin-web exec tsc --noEmit -p tsconfig.app.json` 作通用类型门。

## Tech Stack

- React 19 + TypeScript 5.7（strict，`noUnusedLocals`/`noUnusedParameters` 开）、Vite 6、zustand 5。
- Tailwind 4 + shadcn/ui（深色 token：`bg-background`/`text-foreground`/`text-muted-foreground`/`border-border`/`bg-primary`/`text-destructive`/`bg-online` 等），图标 `lucide-react`。
- 包管理器 **pnpm**。测试：**vitest**（本计划新增）。
- 类型门：`tsc --noEmit -p tsconfig.app.json`（项目无独立 lint，类型检查即质量门）。

---

## ⚠️ 前置依赖（开始前确认，非本计划改动）

- [ ] **确认计划①已合入**：`src/admin-web/src/lib/types/Message.ts` 含 `{ "type": "chat_message", session_id: string, msg_id: string, text: string }` 与 `{ "type": "set_capture", session_id: string, active: boolean }`；`src/admin-web/src/lib/types/AuditType.ts` 含 `"chat"`。
  - 验证命令：
    ```bash
    grep -c 'chat_message' /data/code/OhMyDesk/src/admin-web/src/lib/types/Message.ts
    grep -c 'set_capture' /data/code/OhMyDesk/src/admin-web/src/lib/types/Message.ts
    grep -c '"chat"' /data/code/OhMyDesk/src/admin-web/src/lib/types/AuditType.ts
    ```
  - 预期输出：三条命令均输出 `1`（或更大）。
  - 若输出 `0`：计划①尚未合入，**暂停本计划**。不要手写这些类型——它们是 ts-rs 生成物（文件头 `Do not edit this file manually.`），手写会与后续重导出冲突且违反项目红线。
- [ ] **确认 `set_capture` 字段名**：以 `Message.ts` 实际生成的字段为准（spec §4 记为 `SetCapture { session_id, active }`）。本计划步骤 7 的代码假定字段为 `session_id: string, active: boolean`；若计划①最终用了不同字段名（如 `paused`），按生成类型调整步骤 7 的 payload 字段，不要反过来改类型。

---

## 任务清单

### 步骤 1：引入 vitest 最小测试设施（前置）

**取舍说明**：admin-web 现无任何测试框架。本计划的 4 处纯逻辑（chat 下行 reducer、fileProgress 累计、audit `summarize` 扩展、命令历史游标）是可单测的纯函数级逻辑，值得 TDD。vitest 与既有 Vite/TS 基建同源、零额外配置成本，故引入而非退化为「仅类型检查 + 手测」。引入后这些逻辑走完整 TDD（先 RED 再实现）；UI 渲染仍用人工验证（不引入 jsdom 组件测试，避免过度工程，符合 spec §13「务实降标」）。

- [ ] 安装 vitest（仅 devDependency，node 环境，无需 jsdom）：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web add -D vitest@^3
  ```
- [ ] 在 `src/admin-web/package.json` 的 `scripts` 增加（在 `"preview"` 行后）：
  ```json
    "test": "vitest run",
    "test:watch": "vitest"
  ```
  改完后 `scripts` 块完整应为：
  ```json
    "scripts": {
      "dev": "vite",
      "build": "tsc -b && vite build",
      "preview": "vite preview",
      "test": "vitest run",
      "test:watch": "vitest"
    },
  ```
- [ ] 新建 `src/admin-web/vitest.config.ts`，完整内容：
  ```ts
  import { defineConfig } from "vitest/config";
  import { fileURLToPath } from "node:url";

  // 复用 tsconfig 的 @ 路径别名；node 环境（纯逻辑单测，不渲染组件）。
  export default defineConfig({
    test: {
      environment: "node",
      include: ["src/**/*.test.ts"],
    },
    resolve: {
      alias: {
        "@": fileURLToPath(new URL("./src", import.meta.url)),
      },
    },
  });
  ```
- [ ] 冒烟验证 vitest 可运行（无测试时应优雅退出）：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：vitest 启动，提示 `No test files found`（或类似），退出码 0。后续步骤补上 `.test.ts` 后此命令将真正跑用例。
- [ ] **检查点**：`pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json` 仍通过（无报错输出即通过）。
- [ ] **commit**：`chore(admin-web): 引入 vitest 最小测试设施`

---

### 步骤 2：store.ts — chat 状态与下行处理（TDD）

抽出**纯函数 reducer** 便于单测：新增 `chatMessages` state（按时间正序，最新在末尾）、`sendChat` action（发 `chat_message` 并本地乐观追加自己的消息）、`onEnvelope` 增 `chat_message` 下行分支（追加对端消息）。会话结束/重置时清空 `chatMessages`。

**先写测试（RED）**。新建 `src/admin-web/src/lib/chat.ts`（纯逻辑，便于单测，store 引用它）：

- [ ] 新建 `src/admin-web/src/lib/chat.ts`，完整内容：
  ```ts
  // 会话内即时消息的纯逻辑：消息条目类型 + 追加 reducer。
  // 抽成纯函数便于单测；store 持有 chatMessages 数组并调用这里的 appendChat。

  // 一条会话消息。mine=true 表示本端（管理员）发出，渲染靠右；false 为对端（被控方）。
  export type ChatEntry = {
    msg_id: string;
    text: string;
    mine: boolean;
    ts: number; // 毫秒级本地时间，仅用于展示/排序
  };

  // 追加一条消息；按 msg_id 去重（自己乐观追加后，server 不回显自己的消息；
  // 但若链路回环或重发，去重避免重复气泡）。返回新数组（不可变更新）。
  export function appendChat(list: ChatEntry[], entry: ChatEntry): ChatEntry[] {
    if (list.some((m) => m.msg_id === entry.msg_id)) return list;
    return [...list, entry];
  }
  ```
- [ ] 新建 `src/admin-web/src/lib/chat.test.ts`，完整内容：
  ```ts
  import { describe, it, expect } from "vitest";
  import { appendChat, type ChatEntry } from "./chat";

  function entry(msg_id: string, text: string, mine: boolean): ChatEntry {
    return { msg_id, text, mine, ts: 0 };
  }

  describe("appendChat", () => {
    it("空列表追加一条", () => {
      const out = appendChat([], entry("m1", "你好", true));
      expect(out).toHaveLength(1);
      expect(out[0]).toMatchObject({ msg_id: "m1", text: "你好", mine: true });
    });

    it("保持时间正序追加在末尾", () => {
      let list: ChatEntry[] = [];
      list = appendChat(list, entry("m1", "first", true));
      list = appendChat(list, entry("m2", "second", false));
      expect(list.map((m) => m.msg_id)).toEqual(["m1", "m2"]);
    });

    it("按 msg_id 去重，不重复追加", () => {
      let list = appendChat([], entry("m1", "你好", true));
      list = appendChat(list, entry("m1", "你好", true));
      expect(list).toHaveLength(1);
    });

    it("返回新数组（不可变）", () => {
      const before: ChatEntry[] = [];
      const after = appendChat(before, entry("m1", "x", true));
      expect(after).not.toBe(before);
      expect(before).toHaveLength(0);
    });
  });
  ```
- [ ] 跑测试确认 **RED → GREEN**（`appendChat` 已实现，应直接 GREEN）：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：`chat.test.ts` 4 个用例全部通过。
- [ ] **接入 store.ts**。在 import 区（约第 17 行 `} from "@/lib/file-transfer";` 之后）新增：
  ```ts
  import { appendChat, type ChatEntry } from "@/lib/chat";
  ```
- [ ] 在 `State` 类型的 state 区新增字段（在 `remoteListError: string | null;` 行之后、`remoteQuality: QualityMode;` 之前，约第 67 行）：
  ```ts
    // 会话内即时消息（时间正序，最新在末尾）
    chatMessages: ChatEntry[];
  ```
- [ ] 在 `State` 类型的 actions 区新增 action 签名（在 `setRemoteQuality: ...` 行之后，约第 88 行）：
  ```ts
    // 远控会话内：发送一条即时消息
    sendChat: (text: string) => void;
  ```
- [ ] 在 store 初始 state 区新增（在 `remoteQuality: "smooth",` 行之后，约第 111 行）：
  ```ts
    chatMessages: [],
  ```
- [ ] 在 `initTransport` 的 `onEnvelope` 分发器内，于 `file_list_resp` 分支（约第 225–237 行）之后、`onEnvelope` 回调闭合 `}` 之前，新增下行分支：
  ```ts
      // 会话内即时消息下行：对端（被控方）发来的消息 → 追加（mine=false）
      if (p.type === "chat_message") {
        set((s) => ({
          chatMessages: appendChat(s.chatMessages, {
            msg_id: p.msg_id,
            text: p.text,
            mine: false,
            ts: Date.now(),
          }),
        }));
        return;
      }
  ```
- [ ] 在 `resetRemote` action 的 `set({...})` 内补清空（在 `remoteListError: null,` 行之后，约第 316 行）：
  ```ts
        chatMessages: [],
  ```
- [ ] 在 `endRemote` action 的 `set({...})` 内补清空（约第 299 行，把 `remoteFrame: null` 行扩展）：
  ```ts
      set({ remotePhase: "launch", remoteSessionId: null, remoteFrame: null, chatMessages: [] });
  ```
- [ ] 在 `session_end` 下行分支（约第 161–168 行）的 `set({...})` 内补清空（避免会话被对端结束后残留旧消息）：
  ```ts
        set({
          remotePhase: "rejected",
          remoteRejectReason: "对方已结束远程会话",
          remoteFrame: null,
          chatMessages: [],
        });
  ```
- [ ] 在 store 末尾 actions 区新增 `sendChat` 实现（在 `setRemoteQuality(mode) {...}` action 之后，闭合 `}))` 之前）：
  ```ts
    // 在已授权远控会话内发送一条即时消息：乐观本地追加（mine=true）+ 发 chat_message。
    // server 不回显自己的消息（route_to_peer 只发对端），故本端消息靠乐观追加显示。
    sendChat(text) {
      const sessionId = get().remoteSessionId;
      const trimmed = text.trim();
      if (!sessionId || !trimmed) return;
      const msg_id = genId("c");
      set((s) => ({
        chatMessages: appendChat(s.chatMessages, {
          msg_id,
          text: trimmed,
          mine: true,
          ts: Date.now(),
        }),
      }));
      get().sendEnvelope({
        type: "chat_message",
        session_id: sessionId,
        msg_id,
        text: trimmed,
      });
    },
  ```
- [ ] **检查点**：类型门通过：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json
  ```
  - 预期：无输出（通过）。若报 `chat_message` 不在 `Message` 联合内 → 前置依赖（计划①）未满足，回到前置依赖检查。
- [ ] **commit**：`feat(admin-web): store 增 chatMessages/sendChat 与 chat_message 下行处理`

---

### 步骤 3：store.ts — fileProgress 进度累计（TDD）

把 `FilePanel` 的纯文字 `fileNotice` 升级为 per-file 百分比。新增 `fileProgress` state（`transfer_id → { name, done, total, dir, failed }`），在 push（发分片时累加 done）与 pull（下行 `file_chunk` 累加 done）两路累计，`file_done`/末块标记完成，`file_error` 标失败。进度计算逻辑抽纯函数单测。

**先写测试（RED）**。新建 `src/admin-web/src/lib/file-progress.ts`：

- [ ] 新建 `src/admin-web/src/lib/file-progress.ts`，完整内容：
  ```ts
  // 文件传输进度的纯逻辑：进度条目类型 + 百分比计算 + 累计/完成/失败 reducer。
  // 抽成纯函数便于单测；store 持有 fileProgress 映射并调用这里的 reducer。

  import type { FileDir } from "@/lib/types/FileDir";

  // 一笔在途/完成的传输。done/total 为字节数；failed 与 done>=total(完成) 互斥语义由 UI 解读。
  export type FileProgress = {
    transfer_id: string;
    name: string;
    done: number;
    total: number; // 0 表示未知大小（pull 首包可能未带 total）
    dir: FileDir;
    failed: boolean;
  };

  export type ProgressMap = Record<string, FileProgress>;

  // 百分比（0–100 整数）。total<=0（未知）时返回 null（UI 显示不确定态）。
  export function percent(p: FileProgress): number | null {
    if (p.total <= 0) return null;
    return Math.min(100, Math.round((p.done / p.total) * 100));
  }

  // 开始一笔传输（push file_open / pull file_open）：建条目。
  export function startProgress(
    map: ProgressMap,
    args: { transfer_id: string; name: string; total: number; dir: FileDir },
  ): ProgressMap {
    return {
      ...map,
      [args.transfer_id]: {
        transfer_id: args.transfer_id,
        name: args.name,
        done: 0,
        total: args.total,
        dir: args.dir,
        failed: false,
      },
    };
  }

  // 累加已传字节（push 每发一片 / pull 每收一片）。未知条目忽略。
  export function advanceProgress(map: ProgressMap, transfer_id: string, bytes: number): ProgressMap {
    const cur = map[transfer_id];
    if (!cur) return map;
    return { ...map, [transfer_id]: { ...cur, done: cur.done + bytes } };
  }

  // 标记完成（done 拉满到 total，total 未知时用当前 done）。
  export function completeProgress(map: ProgressMap, transfer_id: string): ProgressMap {
    const cur = map[transfer_id];
    if (!cur) return map;
    return {
      ...map,
      [transfer_id]: { ...cur, done: cur.total > 0 ? cur.total : cur.done },
    };
  }

  // 标记失败。
  export function failProgress(map: ProgressMap, transfer_id: string): ProgressMap {
    const cur = map[transfer_id];
    if (!cur) return map;
    return { ...map, [transfer_id]: { ...cur, failed: true } };
  }
  ```
- [ ] 新建 `src/admin-web/src/lib/file-progress.test.ts`，完整内容：
  ```ts
  import { describe, it, expect } from "vitest";
  import {
    percent,
    startProgress,
    advanceProgress,
    completeProgress,
    failProgress,
    type ProgressMap,
  } from "./file-progress";

  describe("file-progress", () => {
    it("start 建立条目，初始 done=0、未失败", () => {
      const m = startProgress({}, { transfer_id: "t1", name: "a.txt", total: 200, dir: "push" });
      expect(m.t1).toMatchObject({ name: "a.txt", done: 0, total: 200, dir: "push", failed: false });
    });

    it("advance 累加字节，percent 取整 0–100", () => {
      let m: ProgressMap = startProgress({}, { transfer_id: "t1", name: "a", total: 200, dir: "push" });
      m = advanceProgress(m, "t1", 50);
      expect(percent(m.t1)).toBe(25);
      m = advanceProgress(m, "t1", 50);
      expect(percent(m.t1)).toBe(50);
    });

    it("percent 封顶 100（done 溢出 total）", () => {
      let m = startProgress({}, { transfer_id: "t1", name: "a", total: 100, dir: "push" });
      m = advanceProgress(m, "t1", 150);
      expect(percent(m.t1)).toBe(100);
    });

    it("total 未知（<=0）时 percent 返回 null", () => {
      const m = startProgress({}, { transfer_id: "t1", name: "a", total: 0, dir: "pull" });
      expect(percent(m.t1)).toBeNull();
    });

    it("complete 把 done 拉满到 total", () => {
      let m = startProgress({}, { transfer_id: "t1", name: "a", total: 200, dir: "push" });
      m = advanceProgress(m, "t1", 30);
      m = completeProgress(m, "t1");
      expect(percent(m.t1)).toBe(100);
    });

    it("fail 标记失败位", () => {
      let m = startProgress({}, { transfer_id: "t1", name: "a", total: 200, dir: "push" });
      m = failProgress(m, "t1");
      expect(m.t1.failed).toBe(true);
    });

    it("advance/complete/fail 对未知 transfer_id 是无操作", () => {
      const m: ProgressMap = {};
      expect(advanceProgress(m, "x", 10)).toBe(m);
      expect(completeProgress(m, "x")).toBe(m);
      expect(failProgress(m, "x")).toBe(m);
    });
  });
  ```
- [ ] 跑测试确认 GREEN：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：`file-progress.test.ts` 7 个用例全部通过。
- [ ] **接入 store.ts**。import 区新增（在步骤 2 的 chat import 之后）：
  ```ts
  import {
    startProgress,
    advanceProgress,
    completeProgress,
    failProgress,
    type ProgressMap,
  } from "@/lib/file-progress";
  ```
- [ ] `State` 类型 state 区新增（在 `chatMessages: ChatEntry[];` 之后）：
  ```ts
    // 文件传输进度：transfer_id → 进度条目（push + pull 共用）
    fileProgress: ProgressMap;
  ```
- [ ] store 初始 state 区新增（在 `chatMessages: [],` 之后）：
  ```ts
    fileProgress: {},
  ```
- [ ] **下行处理接入**。修改 `file_open` 分支（约第 191–195 行），在建 pullBuffers 后同时建进度条目（pull 回流首包带 `size`，但 `file_open` TS 类型的 `size` 字段存在；若该下行 `file_open` 不含 size 则按 0 未知态）：
  ```ts
      // 取回（pull）回流首包：开缓冲 + 开进度条目
      if (p.type === "file_open") {
        pullBuffers.set(p.transfer_id, { name: p.name, parts: [] });
        set((s) => ({
          fileProgress: startProgress(s.fileProgress, {
            transfer_id: p.transfer_id,
            name: p.name,
            total: Number(p.size),
            dir: "pull",
          }),
        }));
        return;
      }
  ```
- [ ] 修改 `file_chunk` 下行分支（约第 198–209 行）累加 pull 进度，末块完成：
  ```ts
      // 取回数据块：累积 + 进度推进，末块触发浏览器下载并标完成
      if (p.type === "file_chunk") {
        const buf = pullBuffers.get(p.transfer_id);
        if (buf) {
          const bytes = p.data ? b64ToBytes(p.data) : new Uint8Array();
          if (p.data) buf.parts.push(bytes);
          set((s) => ({
            fileProgress: advanceProgress(s.fileProgress, p.transfer_id, bytes.length),
          }));
          if (p.last) {
            pullBuffers.delete(p.transfer_id);
            downloadBytes(buf.name, buf.parts);
            set((s) => ({ fileProgress: completeProgress(s.fileProgress, p.transfer_id) }));
          }
        }
        return;
      }
  ```
- [ ] 修改 `file_error` 下行分支（约第 212–216 行）标失败（保留 fileNotice 兜底文案）：
  ```ts
      // 传输失败：标进度失败 + 文字兜底
      if (p.type === "file_error") {
        pullBuffers.delete(p.transfer_id);
        set((s) => ({
          fileProgress: failProgress(s.fileProgress, p.transfer_id),
          fileNotice: `传输失败：${p.reason}`,
        }));
        return;
      }
  ```
- [ ] 修改 `file_done` 下行分支（约第 219–222 行）push 完成时标完成（push 的 transfer_id 同样登记在 fileProgress，下一步在 `pushFile` 里 start）：
  ```ts
      // push 下发落盘回执：标完成 + 文字告知最终绝对路径
      if (p.type === "file_done") {
        set((s) => ({
          fileProgress: completeProgress(s.fileProgress, p.transfer_id),
          fileNotice: `已下发到被控端：${p.path}`,
        }));
        return;
      }
  ```
- [ ] **push 上行接入**。修改 `pushFile` action（约第 350–391 行）：发 `file_open` 后 start 进度，每发一片 advance：
  ```ts
    async pushFile(file, dest) {
      const sessionId = get().remoteSessionId;
      if (!sessionId) return;
      const transfer_id = genId("t");
      const buf = new Uint8Array(await file.arrayBuffer());
      set((s) => ({
        fileProgress: startProgress(s.fileProgress, {
          transfer_id,
          name: file.name,
          total: buf.length,
          dir: "push",
        }),
      }));
      get().sendEnvelope({
        type: "file_open",
        session_id: sessionId,
        transfer_id,
        name: file.name,
        size: BigInt(buf.length),
        dir: "push",
        dest: dest && dest.trim() ? dest : null,
      });
      if (buf.length === 0) {
        get().sendEnvelope({
          type: "file_chunk",
          session_id: sessionId,
          transfer_id,
          seq: 0n,
          data: "",
          last: true,
        });
      } else {
        let seq = 0;
        for (let off = 0; off < buf.length; off += CHUNK_SIZE) {
          const slice = buf.subarray(off, Math.min(off + CHUNK_SIZE, buf.length));
          const last = off + CHUNK_SIZE >= buf.length;
          get().sendEnvelope({
            type: "file_chunk",
            session_id: sessionId,
            transfer_id,
            seq: BigInt(seq),
            data: bytesToB64(slice),
            last,
          });
          set((s) => ({ fileProgress: advanceProgress(s.fileProgress, transfer_id, slice.length) }));
          seq++;
        }
      }
    },
  ```
  - 注意：本步骤删除了原 `pushFile` 里的两处 `set({ fileNotice: ... })`（「正在下发」「已发送…等待写入确认」），进度由 `fileProgress` 接管显示；最终落盘路径仍由 `file_done` 的 `fileNotice` 告知。
- [ ] `resetRemote` 与 `endRemote` 补清空 `fileProgress`：
  - `resetRemote` 的 `set({...})` 内新增 `fileProgress: {},`
  - `endRemote` 的 `set({...})` 内新增 `fileProgress: {},`
- [ ] **检查点**：类型门 + 测试：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json && pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：tsc 无输出；vitest chat + file-progress 共 11 用例通过。
- [ ] **commit**：`feat(admin-web): store 增 fileProgress 进度累计（push/pull 双路）`

---

### 步骤 4：ChatPanel 组件 + remote-session 第 4 标签（手动验证）

在 `remote-tools.tsx` 新增 `ChatPanel`（复用 `TabButton`、深色 token、`inputCls` 风格）；在 `remote-session.tsx` 把 `ToolTab` 扩为 4 态并加第 4 标签，未读红点。

- [ ] **新增 ChatPanel**。在 `src/admin-web/src/components/control/remote-tools.tsx` 末尾（`RemotePane` 之后）追加：
  ```tsx
  // ── 会话消息标签页：会话内双向即时消息（左对端 / 右本端） ───────────────────────
  export function ChatPanel() {
    const chatMessages = useStore((s) => s.chatMessages);
    const sendChat = useStore((s) => s.sendChat);
    const [text, setText] = useState("");
    const endRef = useRef<HTMLDivElement>(null);

    // 新消息自动滚到底（spec §9.4）。
    useEffect(() => {
      endRef.current?.scrollIntoView({ block: "end" });
    }, [chatMessages.length]);

    return (
      <div className="flex h-full flex-col p-3">
        <div className="flex min-h-0 flex-1 flex-col gap-2 overflow-y-auto">
          {chatMessages.length === 0 ? (
            <p className="text-xs text-muted-foreground">会话内即时消息。发送的消息将实时送达被控方，并全文写入审计。</p>
          ) : (
            chatMessages.map((m) => (
              <div
                key={m.msg_id}
                className={"flex " + (m.mine ? "justify-end" : "justify-start")}
              >
                <div
                  className={
                    "max-w-[75%] whitespace-pre-wrap break-words rounded-lg px-3 py-1.5 text-xs " +
                    (m.mine
                      ? "bg-primary text-primary-foreground"
                      : "border border-border bg-background text-foreground")
                  }
                >
                  {m.text}
                </div>
              </div>
            ))
          )}
          <div ref={endRef} />
        </div>

        <form
          onSubmit={(e) => {
            e.preventDefault();
            sendChat(text);
            setText("");
          }}
          className="mt-2 flex shrink-0 gap-1.5"
        >
          <input
            value={text}
            onChange={(e) => setText(e.target.value)}
            placeholder="输入消息，回车发送…"
            className={inputCls}
            spellCheck={false}
            autoComplete="off"
          />
          <Button type="submit" size="sm" disabled={!text.trim()}>
            发送
          </Button>
        </form>
      </div>
    );
  }
  ```
- [ ] **更新 import**（`remote-tools.tsx` 第 1 行）把 `useRef` 加入 React import：
  ```tsx
  import { useEffect, useRef, useState } from "react";
  ```
- [ ] **接入 remote-session.tsx**。修改 import（约第 16 行）补 `ChatPanel`，并补图标 `MessageSquare`（第 2 行 lucide import）：
  ```tsx
  import { Camera, Maximize, PhoneOff, TriangleAlert, Monitor, Terminal, FolderTree, MessageSquare } from "lucide-react";
  ```
  ```tsx
  import { CommandPanel, FilePanel, ChatPanel, TabButton } from "@/components/control/remote-tools";
  ```
- [ ] 扩展 `ToolTab` 类型（约第 18 行）：
  ```tsx
  type ToolTab = "remote" | "cmd" | "file" | "chat";
  ```
- [ ] 在组件内订阅 chatMessages 以驱动未读红点（在 `const remoteQuality = ...` 等订阅之后，约第 44 行）：
  ```tsx
    const chatCount = useStore((s) => s.chatMessages.length);
    // 上次停留在「会话消息」标签时已读到的消息数；切到 chat 标签即清零未读。
    const [readChatCount, setReadChatCount] = useState(0);
    const unreadChat = tab === "chat" ? 0 : Math.max(0, chatCount - readChatCount);
  ```
  - 注意：`tab` 在此行之前已声明（约第 47 行 `const [tab, setTab] = useState<ToolTab>("remote");`）。**把 `chatCount`/`readChatCount`/`unreadChat` 这三行移到 `tab` 声明之后**（即放在第 47 行 `useState<ToolTab>` 那行的紧后面），避免 TDZ。
- [ ] 切到 chat 标签时清未读：在第 4 标签 `TabButton` 的 `onClick` 里调用 `setReadChatCount(chatCount)`（见下条）。
- [ ] 在标签栏 `<nav>`（约第 243–253 行）的「文件传输」`TabButton` 之后新增第 4 标签：
  ```tsx
        <TabButton
          active={tab === "chat"}
          onClick={() => { setTab("chat"); setReadChatCount(chatCount); }}
          icon={<MessageSquare className="size-3.5" />}
        >
          <span className="flex items-center gap-1.5">
            会话消息
            {unreadChat > 0 && (
              <span className="flex min-w-4 items-center justify-center rounded-full bg-destructive px-1 text-[10px] font-medium leading-4 text-destructive-foreground">
                {unreadChat}
              </span>
            )}
          </span>
        </TabButton>
  ```
- [ ] 在主体区（约第 293–302 行）`{tab === "file" && (...)}` 之后新增：
  ```tsx
          {tab === "chat" && (
            <div className="min-h-0 flex-1">
              <ChatPanel />
            </div>
          )}
  ```
- [ ] **检查点**：类型门通过：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json
  ```
- [ ] **手动验证**（`pnpm -C /data/code/OhMyDesk/src/admin-web dev`，建立一个远控会话后）：
  - 顶部出现第 4 标签「会话消息」，图标为对话气泡。
  - 切到该标签，输入框可打字（不被全局键盘捕获吞掉——已有 `isEditableTarget` 守卫覆盖 INPUT）。
  - 回车/点「发送」后，自己的消息以**靠右**主色气泡出现；输入框清空。
  - 模拟对端回消息（被控端或后端回灌 `chat_message`）时，对端消息以**靠左**边框气泡出现。
  - 停留在非「会话消息」标签时收到新消息 → 「会话消息」标签出现红点数字；切到该标签红点清零。
  - 新消息时消息流自动滚到底。
- [ ] **commit**：`feat(admin-web): 新增 ChatPanel 与会话消息第 4 标签（含未读红点）`

---

### 步骤 5：CommandPanel 命令历史 ↑↓ 回溯（TDD + 手动验证）

`CommandPanel` 输入框支持 ↑↓ 调出历史命令。历史游标逻辑（纯函数）单测；UI 接线手测。

**先写测试（RED）**。新建 `src/admin-web/src/lib/cmd-history.ts`：

- [ ] 新建 `src/admin-web/src/lib/cmd-history.ts`，完整内容：
  ```ts
  // 命令历史回溯的纯逻辑：给定历史数组（最新在末尾）+ 当前游标，计算 ↑/↓ 后的新游标与回填文本。
  // 游标语义：null = 不在浏览历史（停在用户当前输入）；0..len-1 = 指向 history[idx]。
  // ↑ 越往旧走（idx 减小，最旧为 0）；↓ 越往新走，越过最新回到 null（清空到当前输入）。

  export type HistoryNav = { cursor: number | null; text: string | null };
  // text=null 表示「保持输入框现有文本不变」；string 表示「回填为该文本」。

  export function navPrev(history: string[], cursor: number | null): HistoryNav {
    if (history.length === 0) return { cursor: null, text: null };
    // 初次按 ↑：从最新一条（末尾）开始。
    const next = cursor === null ? history.length - 1 : Math.max(0, cursor - 1);
    return { cursor: next, text: history[next] };
  }

  export function navNext(history: string[], cursor: number | null): HistoryNav {
    if (cursor === null) return { cursor: null, text: null };
    const next = cursor + 1;
    if (next >= history.length) return { cursor: null, text: "" }; // 越过最新 → 回到空白当前输入
    return { cursor: next, text: history[next] };
  }

  // 提交一条命令后追加进历史（去掉与上一条完全相同的连续重复），返回新历史。
  export function pushHistory(history: string[], cmd: string): string[] {
    const t = cmd.trim();
    if (!t) return history;
    if (history.length > 0 && history[history.length - 1] === t) return history;
    return [...history, t];
  }
  ```
- [ ] 新建 `src/admin-web/src/lib/cmd-history.test.ts`，完整内容：
  ```ts
  import { describe, it, expect } from "vitest";
  import { navPrev, navNext, pushHistory } from "./cmd-history";

  describe("cmd-history", () => {
    const h = ["ls", "pwd", "whoami"]; // 最新在末尾

    it("↑ 初次从最新一条开始", () => {
      expect(navPrev(h, null)).toEqual({ cursor: 2, text: "whoami" });
    });

    it("连续 ↑ 往旧走，封底在最旧", () => {
      expect(navPrev(h, 2)).toEqual({ cursor: 1, text: "pwd" });
      expect(navPrev(h, 1)).toEqual({ cursor: 0, text: "ls" });
      expect(navPrev(h, 0)).toEqual({ cursor: 0, text: "ls" }); // 封底
    });

    it("空历史 ↑ 无操作", () => {
      expect(navPrev([], null)).toEqual({ cursor: null, text: null });
    });

    it("↓ 往新走，越过最新回到空白当前输入", () => {
      expect(navNext(h, 0)).toEqual({ cursor: 1, text: "pwd" });
      expect(navNext(h, 2)).toEqual({ cursor: null, text: "" });
    });

    it("↓ 在 null（未浏览）时无操作", () => {
      expect(navNext(h, null)).toEqual({ cursor: null, text: null });
    });

    it("pushHistory 追加在末尾、去连续重复、忽略空白", () => {
      expect(pushHistory(h, "ls -al")).toEqual(["ls", "pwd", "whoami", "ls -al"]);
      expect(pushHistory(h, "whoami")).toEqual(h); // 与上一条重复
      expect(pushHistory(h, "   ")).toEqual(h); // 空白忽略
    });
  });
  ```
- [ ] 跑测试确认 GREEN：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：`cmd-history.test.ts` 6 用例通过。
- [ ] **接入 CommandPanel**（`remote-tools.tsx`）。在文件顶部 import 区（`import { useStore }` 之后）新增：
  ```tsx
  import { navPrev, navNext, pushHistory } from "@/lib/cmd-history";
  ```
- [ ] 重写 `CommandPanel` 组件（约第 91–153 行），把表单提交与 ↑↓ 键接上历史。新 `CommandPanel` 完整 body：
  ```tsx
  export function CommandPanel() {
    const execResults = useStore((s) => s.execResults);
    const execCommand = useStore((s) => s.execCommand);
    const [cmd, setCmd] = useState("");
    const [history, setHistory] = useState<string[]>([]);
    const [cursor, setCursor] = useState<number | null>(null);

    function submit() {
      const t = cmd.trim();
      if (!t) return;
      execCommand(t);
      setHistory((h) => pushHistory(h, t));
      setCursor(null);
      setCmd("");
    }

    function onKeyDown(e: React.KeyboardEvent<HTMLInputElement>) {
      if (e.key === "ArrowUp") {
        const nav = navPrev(history, cursor);
        if (nav.text !== null) {
          e.preventDefault();
          setCursor(nav.cursor);
          setCmd(nav.text);
        }
      } else if (e.key === "ArrowDown") {
        const nav = navNext(history, cursor);
        if (nav.text !== null) {
          e.preventDefault();
          setCursor(nav.cursor);
          setCmd(nav.text);
        }
      }
    }

    return (
      <div className="flex h-full flex-col p-3">
        <form
          onSubmit={(e) => {
            e.preventDefault();
            submit();
          }}
          className="flex shrink-0 gap-1.5"
        >
          <input
            value={cmd}
            onChange={(e) => {
              setCmd(e.target.value);
              setCursor(null); // 手动编辑即脱离历史浏览
            }}
            onKeyDown={onKeyDown}
            placeholder="whoami / ipconfig / ls -al（↑↓ 调历史）"
            className={inputCls}
            spellCheck={false}
            autoComplete="off"
          />
          <Button type="submit" size="sm" disabled={!cmd.trim()}>
            执行
          </Button>
        </form>

        <div className="mt-2 flex min-h-0 flex-1 flex-col gap-2 overflow-y-auto">
          {execResults.length === 0 && (
            <p className="text-xs text-muted-foreground">在被控端系统 shell 中执行一次性命令，回传输出。</p>
          )}
          {execResults.map((e) => (
            <div key={e.exec_id} className="rounded-md border border-border bg-background p-2 text-xs">
              <div className="break-all font-mono text-foreground">$ {e.command}</div>
              {e.pending ? (
                <div className="mt-1 flex items-center gap-1 text-muted-foreground">
                  <Loader2 className="size-3 animate-spin" aria-hidden /> 执行中…
                </div>
              ) : (
                <div className="mt-1 flex flex-col gap-1">
                  {e.stdout && (
                    <pre className="max-h-40 overflow-auto whitespace-pre-wrap break-all font-mono text-foreground">
                      {e.stdout}
                    </pre>
                  )}
                  {e.stderr && (
                    <pre className="max-h-40 overflow-auto whitespace-pre-wrap break-all font-mono text-destructive">
                      {e.stderr}
                    </pre>
                  )}
                  <div className="text-muted-foreground">
                    退出码 {e.exit_code ?? "—"} · {e.duration_ms}ms
                    {e.truncated && " · 输出已截断"}
                  </div>
                </div>
              )}
            </div>
          ))}
        </div>
      </div>
    );
  }
  ```
- [ ] **检查点**：类型门 + 测试：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json && pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：tsc 无输出；vitest 至此累计 chat+file-progress+cmd-history 共 17 用例通过。
- [ ] **手动验证**（`dev`，会话内「命令行」标签）：
  - 连续执行 2–3 条命令（如 `whoami`、`ls`、`pwd`）。
  - 焦点在输入框按 ↑：依次回填最新→更旧命令；封底在最旧。
  - 按 ↓：依次回填更新命令；越过最新清空输入框。
  - 手动编辑输入框文字后再按 ↑：从最新一条重新开始（游标已因 onChange 重置为 null）。
- [ ] **commit**：`feat(admin-web): 命令行输入框支持 ↑↓ 历史回溯`

---

### 步骤 6：FilePanel 进度条 UI（手动验证）

把 `FilePanel` 底部纯文字 `fileNotice` 升级为 per-file 进度条列表（消费步骤 3 的 `fileProgress`），保留 `fileNotice` 作落盘路径/失败文案兜底。

- [ ] **重写 FilePanel**（`remote-tools.tsx` 约第 156–172 行）。新 body：
  ```tsx
  export function FilePanel() {
    const fileNotice = useStore((s) => s.fileNotice);
    const fileProgress = useStore((s) => s.fileProgress);
    const transfers = Object.values(fileProgress);

    return (
      <div className="flex h-full flex-col">
        <div className="grid min-h-0 flex-1 grid-cols-2 divide-x divide-border">
          <LocalPane />
          <RemotePane />
        </div>

        {(transfers.length > 0 || fileNotice) && (
          <div className="shrink-0 border-t border-border px-3 py-2">
            {transfers.length > 0 && (
              <div className="mb-1.5 flex flex-col gap-1.5">
                {transfers.map((t) => (
                  <TransferBar key={t.transfer_id} t={t} />
                ))}
              </div>
            )}
            {fileNotice && <div className="text-xs text-muted-foreground">{fileNotice}</div>}
          </div>
        )}
      </div>
    );
  }

  // 单条传输进度条：方向标签 + 文件名 + 进度条 + 百分比/状态。
  function TransferBar({ t }: { t: FileProgress }) {
    const pct = percent(t);
    const dirLabel = t.dir === "push" ? "下发" : "取回";
    return (
      <div className="flex items-center gap-2 text-xs">
        <span className="shrink-0 text-muted-foreground">{dirLabel}</span>
        <span className="min-w-0 max-w-[40%] truncate text-foreground" title={t.name}>
          {t.name}
        </span>
        <div className="relative h-1.5 min-w-0 flex-1 overflow-hidden rounded-full bg-secondary">
          <div
            className={
              "absolute inset-y-0 left-0 rounded-full transition-[width] duration-200 " +
              (t.failed ? "bg-destructive" : "bg-primary")
            }
            style={{ width: t.failed ? "100%" : pct === null ? "100%" : `${pct}%` }}
          />
        </div>
        <span className="w-12 shrink-0 text-right font-mono text-[11px] text-muted-foreground">
          {t.failed ? "失败" : pct === null ? "…" : `${pct}%`}
        </span>
      </div>
    );
  }
  ```
- [ ] **更新 import**（`remote-tools.tsx`）。在 cmd-history import 之后新增（消费进度纯函数与类型）：
  ```tsx
  import { percent, type FileProgress } from "@/lib/file-progress";
  ```
- [ ] **检查点**：类型门：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json
  ```
- [ ] **手动验证**（`dev`，会话内「文件传输」标签）：
  - 左栏选一个文件点「下发」→ 底部出现 `下发 文件名 ▓▓▓░ NN%`，进度推进到 100%；随后出现 `已下发到被控端：<路径>` 文字。
  - 右栏对一个文件点「取回」→ 底部出现 `取回 文件名 …/NN%`，完成后浏览器触发下载。
  - 制造失败（断开被控端或传超限文件）→ 进度条变红显「失败」，并有 `传输失败：…` 文字。
- [ ] **commit**：`feat(admin-web): 文件传输进度条（替换纯文字提示）`

---

### 步骤 7：懒推流 — 切标签发 set_capture（手动验证）

admin 离开「远程控制」标签发 `set_capture{active:false}`，回到「远程控制」标签发 `set_capture{active:true}`，与 Slint 端一致，省内网带宽。

- [ ] **store 增 action**（可选但推荐封装；也可直接在组件用 `sendEnvelope`）。本计划采用「组件内直接 `sendEnvelope`」以最小化 store 表面，逻辑简单无需单测。
- [ ] **接入 remote-session.tsx**。在已有的 `sendEnvelope` 订阅（约第 42 行）与 `tab` 状态（约第 47 行）基础上，新增一个 effect：当 `tab`/`remoteSessionId` 变化时，按是否在「远程控制」标签发 capture 开关。放在键盘 effect（约第 167 行结束）之后：
  ```tsx
    // 懒推流：仅「远程控制」标签需要桌面帧。进入该标签 → 恢复推流；离开 → 暂停（省内网带宽）。
    // 与 Slint 端一致，复用 set_capture 协议（计划①导出）。
    useEffect(() => {
      if (!remoteSessionId) return;
      sendEnvelope({ type: "set_capture", session_id: remoteSessionId, active: tab === "remote" });
    }, [tab, remoteSessionId, sendEnvelope]);
  ```
  - 说明：连接成功默认 `tab==="remote"`，effect 首跑即发 `active:true`（恢复/确认推流），符合 spec §3「默认落在远程桌面标签」。切走任一其他标签发 `active:false`，切回发 `active:true`。
- [ ] **检查点**：类型门：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json
  ```
  - 预期：无输出。若报 `set_capture` 不在 `Message` 内或字段名不符 → 回到「前置依赖」核对计划① 生成的 `Message.ts` 字段名，按生成类型调整本步 payload。
- [ ] **手动验证**（`dev` + 浏览器 DevTools Network/WS 面板，会话内）：
  - 连接成功默认「远程控制」标签 → WS 出帧（`frame`）持续。
  - 切到「命令行」/「文件传输」/「会话消息」→ WS 发出一条 `{"type":"set_capture",...,"active":false}`，帧停止。
  - 切回「远程控制」→ WS 发出 `{"type":"set_capture",...,"active":true}`，帧恢复。
  - 在非桌面标签操作命令/文件/消息时，键鼠不被转发到被控端（已有 `tab !== "remote"` 守卫）。
- [ ] **commit**：`feat(admin-web): 懒推流——切标签发 set_capture 启停推流`

---

### 步骤 8：audit.ts summarize 扩展 command/file/chat（TDD）

`summarize` 现仅统计 `screenshot`/`input`，扩展识别 `command`/`file_transfer`/`chat`，进审计表「操作摘要」列。

**先写测试（RED）**。`summarize` 当前是模块私有函数；本步骤将其 `export` 以便单测。

- [ ] **导出 summarize**（`src/admin-web/src/lib/adapters/audit.ts` 约第 56 行）把 `function summarize` 改为 `export function summarize`。
- [ ] **扩展 summarize 实现**（替换约第 56–63 行）：
  ```ts
  export function summarize(items: TimelineItem[]): string {
    const count = (kind: AuditType) => items.filter((i) => i.kind === kind).length;
    const shots = count("screenshot");
    const inputs = items.filter((i) => i.kind === "input");
    const commands = count("command");
    const files = count("file_transfer");
    const chats = count("chat");
    const parts: string[] = [];
    if (shots > 0) parts.push(`截图 ${shots} 次`);
    if (inputs.length > 0) parts.push(inputs.map((i) => i.text).join("、"));
    if (commands > 0) parts.push(`命令 ${commands} 条`);
    if (files > 0) parts.push(`文件传输 ${files} 次`);
    if (chats > 0) parts.push(`消息 ${chats} 条`);
    return parts.join("，") || "无操作记录";
  }
  ```
- [ ] 新建 `src/admin-web/src/lib/adapters/audit.test.ts`，完整内容：
  ```ts
  import { describe, it, expect } from "vitest";
  import { summarize, type TimelineItem } from "./audit";

  function item(kind: TimelineItem["kind"], text = ""): TimelineItem {
    return { ts: 0, kind, text };
  }

  describe("summarize", () => {
    it("空 → 无操作记录", () => {
      expect(summarize([])).toBe("无操作记录");
    });

    it("统计截图次数", () => {
      expect(summarize([item("screenshot"), item("screenshot")])).toBe("截图 2 次");
    });

    it("input 用其 text 直拼", () => {
      expect(summarize([item("input", "输入操作 47 次")])).toBe("输入操作 47 次");
    });

    it("识别 command/file_transfer/chat 并计数", () => {
      const items = [
        item("command"),
        item("command"),
        item("file_transfer"),
        item("chat"),
        item("chat"),
        item("chat"),
      ];
      expect(summarize(items)).toBe("命令 2 条，文件传输 1 次，消息 3 条");
    });

    it("混合类型按固定顺序拼接", () => {
      const items = [
        item("screenshot"),
        item("input", "输入操作 5 次"),
        item("command"),
        item("file_transfer"),
        item("chat"),
      ];
      expect(summarize(items)).toBe("截图 1 次，输入操作 5 次，命令 1 条，文件传输 1 次，消息 1 条");
    });
  });
  ```
- [ ] 跑测试确认 GREEN：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：`audit.test.ts` 5 用例通过。
  - 前置依赖提醒：若 `AuditType` 尚无 `"chat"`，`item("chat")` 会类型报错 → 计划①未合入，回到前置依赖检查。
- [ ] **检查点**：类型门 + 全量测试：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json && pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：tsc 无输出；vitest 累计 22 用例通过。
- [ ] **commit**：`feat(admin-web): 审计摘要扩展识别 command/file_transfer/chat`

---

### 步骤 9：audit-cells / timeline 图标文案补全（手动验证）

`audit-timeline-sheet.tsx` 的 `KIND_ICON` 已含 `command`/`file_transfer`，但缺 `chat`（`AuditType` 计划①新增了 `"chat"`，`Record<AuditType, ...>` 会因缺键类型报错）。补 `chat` 图标，并给 timeline 中文事件名映射（现直接渲染 `ev.kind` 英文，体验差）。

- [ ] **补 chat 图标**（`audit-timeline-sheet.tsx`）。第 1–10 行 lucide import 加 `MessageSquare`：
  ```tsx
  import {
    Camera,
    FileUp,
    KeyboardIcon,
    LogOut,
    MessageSquare,
    PlugZap,
    ShieldX,
    Terminal,
    type LucideIcon,
  } from "lucide-react";
  ```
- [ ] `KIND_ICON`（约第 31–40 行）补 `chat` 键：
  ```tsx
  const KIND_ICON: Record<AuditType, LucideIcon> = {
    connect: PlugZap,
    screenshot: Camera,
    input: KeyboardIcon,
    auth_fail: ShieldX,
    reject: ShieldX,
    disconnect: LogOut,
    command: Terminal,
    file_transfer: FileUp,
    chat: MessageSquare,
  };
  ```
- [ ] **加中文事件名映射**。在 `KIND_ICON` 之后新增：
  ```tsx
  // 事件类型中文名（timeline 节点标题用，替代直接渲染英文 kind）。
  const KIND_LABEL: Record<AuditType, string> = {
    connect: "建立连接",
    screenshot: "截图",
    input: "输入操作",
    auth_fail: "鉴权失败",
    reject: "拒绝",
    disconnect: "断开",
    command: "执行命令",
    file_transfer: "文件传输",
    chat: "即时消息",
  };
  ```
- [ ] **timeline 节点标题改用中文名**（约第 116 行 `{ev.kind}`）：
  ```tsx
                          {KIND_LABEL[ev.kind]}
  ```
- [ ] **可选：audit-cells.tsx 导出对应图标**（spec §10 要求「audit-cells 加对应图标/文案」）。当前 `audit-cells.tsx` 仅导出 `AuthFailIcon`；timeline 图标已集中在 `audit-timeline-sheet.tsx` 的 `KIND_ICON`，无需在 cells 重复。**判定**：图标已在 timeline sheet 统一管理，cells 无新增需求 → 本条不改 `audit-cells.tsx`，在 Self-Review 记录此取舍（避免双份图标来源，符合 Rams 一致性 + 单一来源）。
- [ ] **检查点**：类型门：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json
  ```
  - 预期：无输出。若此前 `KIND_ICON` 因缺 `chat` 键已报错，本步修复之。
- [ ] **手动验证**（`dev`，会话审计页）：
  - 打开一条有命令/文件/聊天记录的会话 → 点开 timeline 抽屉。
  - 命令事件显终端图标 + 「执行命令」；文件事件显上传图标 + 「文件传输」；聊天事件显对话气泡图标 + 「即时消息」，事件正文（`ev.text`）展示命令文本/文件名/消息全文。
  - 审计表「操作摘要」列对相应会话显示「命令 N 条 / 文件传输 N 次 / 消息 N 条」（步骤 8 成果）。
- [ ] **commit**：`feat(admin-web): 审计时间线补 chat 图标与中文事件名`

---

### 步骤 10：构建 + 测试 + 类型门 闭环（验收）

- [ ] **全量测试**：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web test
  ```
  - 预期：4 个测试文件（chat / file-progress / cmd-history / audit）共 22 用例全部通过。
- [ ] **类型门**：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web exec tsc --noEmit -p tsconfig.app.json
  ```
  - 预期：无输出（通过）。
- [ ] **生产构建**（含 `tsc -b` + vite build）：
  ```bash
  pnpm -C /data/code/OhMyDesk/src/admin-web build
  ```
  - 预期：`tsc -b` 无错误，vite 产出 `dist/`，无类型/打包报错。
- [ ] **lint**：项目无 `lint` 脚本、无 eslint 配置（已确认 `package.json` 仅 dev/build/preview，无 eslint 依赖与配置文件）。**取舍**：以 `tsc --noEmit`（strict + noUnusedLocals/Params）作为唯一静态质量门，等价覆盖未用变量/类型错误；不为本计划临时引入 eslint（超范围、与 spec 一致性原则冲突）。在 Self-Review 记录。
- [ ] **端到端手测闭环**（`dev`，模式 A 或 B 各跑一遍，spec §13）：
  - 桌面：连接成功出帧。
  - 命令：执行命令出回执，↑↓ 调历史。
  - 文件：下发/取回各一次，进度条到 100%。
  - 消息：双向收发，未读红点。
  - 懒推流：切标签 WS 发 set_capture，帧启停。
  - 审计：会话结束后审计页摘要含命令/文件/消息计数，timeline 图标文案正确。
- [ ] **commit**（如有收尾微调）：`chore(admin-web): 计划③ 收尾 构建+测试+手测闭环`

---

## Self-Review

### 对照 spec 逐条覆盖

| spec 条目 | 覆盖步骤 | 状态 |
|---|---|---|
| §7.1 文件传输进度条（per-file 百分比，替换纯文字 fileNotice） | 步骤 3（store fileProgress）+ 步骤 6（TransferBar UI） | ✅ 保留 fileNotice 作落盘路径/失败兜底 |
| §7.2 命令历史 ↑↓ 回溯（组件内 history 数组） | 步骤 5（cmd-history 纯逻辑 + CommandPanel 接线） | ✅ |
| §7.3 / §8 会话消息标签 | 步骤 2（store sendChat/chatMessages/下行）+ 步骤 4（ChatPanel + 第 4 标签 + 未读红点） | ✅ 保持标签式，非弹窗 |
| §8 双向 chat_message，全文审计 | 步骤 2（下行 mine=false / 上行乐观 mine=true） | ✅ 审计落库在计划①服务端 |
| §3 懒推流（切标签 set_capture active 开关） | 步骤 7（remote-session effect） | ✅ 与 Slint 端一致 |
| §10 审计 summarize 扩展 command/file/chat | 步骤 8（summarize 导出 + 扩展 + 单测） | ✅ |
| §10 audit-cells / timeline 图标文案 | 步骤 9（KIND_ICON 补 chat + KIND_LABEL 中文名） | ✅ audit-cells 不重复造图标（见取舍） |
| §13 测试策略（务实降标） | 步骤 1（vitest 最小设施）+ 各步 TDD | ✅ 纯逻辑 TDD，UI 手测，不追组件测试 |

### 关键设计决策落实

- **标签式布局**：会话消息是第 4 标签（`ToolTab` 扩为 4 态），未改弹窗。✅
- **状态单一来源**：`chatMessages`/`fileProgress` 全在 store；组件仅持本地 UI 状态（输入文本、命令历史游标、未读基线）。✅
- **TS strict、禁 any**：全程无 `any`；纯逻辑抽 `src/lib/*.ts` 带显式类型；类型门 `tsc --noEmit`。✅
- **跨端类型只用生成物**：`chat_message`/`set_capture`/`chat`/`FileDir` 均引用 `src/lib/types/`，未手写重复实体，未改生成文件。✅（前置依赖核验计划①已重导出）
- **Tailwind + shadcn 深色 token**：复用 `inputCls`、`TabButton`、`bg-primary`/`text-muted-foreground`/`border-border`/`bg-destructive`/`bg-secondary` 等既有 token，无新视觉风格。✅
- **pnpm**：所有命令用 `pnpm -C src/admin-web`。✅

### 占位符扫描

- 全文无 `TODO`/`FIXME`/`...`（省略）/`<占位>`；所有组件与纯逻辑均为完整可粘贴代码。✅
- 所有 import 显式列出；所有新建文件给出完整内容；所有编辑给出锚点行号 + 完整替换块。✅

### 类型一致性

- `ChatEntry`/`FileProgress`/`ProgressMap`/`HistoryNav` 为本计划新增的**本地 UI 派生类型**（非协议实体），定义在 `src/lib/*.ts`，不与 ts-rs 生成物重叠。✅
- 协议字段引用以生成 `Message.ts` 为准：`chat_message{session_id,msg_id,text}`、`set_capture{session_id,active}`、`file_open{...,size,dir,dest}`、`file_chunk{...,seq,data,last}`、`file_done{...,path}`、`file_error{...,reason}`。✅
- 前置风险：`set_capture` 字段名若与计划①最终生成不一致（如 `active` vs `paused`），步骤 7 以生成类型为准调整 payload（已在步骤 7 与前置依赖标注）。⚠️ 已记录。

### 测试设施说明（影响 TDD 力度）

- admin-web **原无测试框架**（`package.json` 仅 dev/build/preview，无 vitest/jest，无 lint 脚本与 eslint 配置）。
- 本计划步骤 1 **引入 vitest 最小配置**（node 环境，复用 @ 别名），对 4 处纯逻辑（chat reducer / fileProgress / 命令历史 / audit summarize）做完整 TDD（22 用例）。
- UI 组件（ChatPanel / 进度条 / 标签 / 审计图标）不引入 jsdom 组件测试，以 `pnpm dev` 手动验证 + `tsc --noEmit` 类型门覆盖，符合 spec §13「务实降标，不追 80%」。
