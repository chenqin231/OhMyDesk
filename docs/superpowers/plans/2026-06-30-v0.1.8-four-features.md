# OhMyDesk v0.1.8 四功能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付剪贴板双向同步、WEB 强制远程、客户端伙伴密码选填、临时密码 UI 改造四功能,共享统一授权模型。

**Architecture:** server 端 `handle_connect_request` 一处用纯函数 `decide_connect` 决定免同意/需同意/拒绝;被控端按 `IncomingControl.auto_accept` 决定弹框或直接进被控态;剪贴板用独占线程 worker(arboard 句柄非 Send)+ 单线程私有 `last_synced` 缓存防回环。

**Tech Stack:** Rust(protocol/server/client,ts-rs/serde/tokio/arboard/Slint)、React+Zustand(admin-web)。

参考设计:`docs/superpowers/specs/2026-06-30-v0.1.8-four-features-design.md`

---

## 文件结构

| 文件 | 职责 | 变更 |
|---|---|---|
| `src/protocol/src/lib.rs` | 协议消息 | +`ConnectRequest.force` +`IncomingControl.auto_accept` +`Message::ClipboardSync` |
| `src/server/src/handlers.rs` | 连接鉴权决策 | +`decide_connect` 纯函数 + auto_accept 分支 |
| `src/server/src/hub.rs` | 消息路由 | ConnectRequest 透传 force + ClipboardSync 路由 |
| `src/client/src/net/mod.rs` | net 类型/旁路桥 | +`FromUi::ClipboardSync` +`ClipboardBridge`/`ClipboardMsg` |
| `src/client/src/net/dispatch.rs` | 收发分发 | IncomingControl auto_accept + ClipboardSync 收发 + 启停点 |
| `src/client/src/workers.rs` | 后台 worker | +`consume_clipboard` +`should_push_clipboard` |
| `src/client/src/main.rs` | 挂载 | +clipboard 通道/worker |
| `src/client/ui/app.slint` | GUI | ValueRow masked + 密码行图标 + 伙伴密码选填 |
| `src/admin-web/src/store.ts` | 前端 store | startRemote +force |
| `src/admin-web/src/components/assets/terminal-assets.tsx` | 资产列表 | 申请远程/强制远程双按钮 + 二次确认 |
| `src/admin-web/src/components/assets/terminal-detail-sheet.tsx` | 详情抽屉 | 文案同步 |
| `src/admin-web/src/lib/types/*.ts` | ts-rs 生成 | `cargo test -p protocol` 重新导出 |

执行建议:Phase A(授权)→ B(剪贴板)→ C(密码 UI),每 Task 结束 `cargo build` 绿 + 提交。建议 worktree 隔离。

---

## Phase A:统一授权模型(功能 2+3)

### Task A1:授权决策纯函数 + server 免同意分支

**Files:**
- Modify: `src/protocol/src/lib.rs:177-188`(ConnectRequest/IncomingControl 加字段)
- Modify: `src/server/src/handlers.rs:30-102`(handle_connect_request 重写)
- Modify: `src/server/src/hub.rs:137-151`(透传 force)
- Test: `src/server/src/handlers.rs`(新增 `#[cfg(test)] mod tests`)

- [ ] **Step 1:协议加字段**

`src/protocol/src/lib.rs`,`ConnectRequest` 与 `IncomingControl` 改为:

```rust
    ConnectRequest {
        mode: Mode,
        target: String,
        password: Option<String>,
        /// WEB 强制远程:免被控端同意直连(仅 admin- 发起方有效,server 端硬校验)。
        #[serde(default)]
        force: bool,
    },
    /// server → 被控端:有主控发起控制,携带 server 生成的 session_id;
    /// 被控端授权后回 AuthResult 带此 session_id(解 task#8 时序缺口,统一会话 id 来源)。
    IncomingControl {
        session_id: String,
        from: String,
        mode: Mode,
        /// true=免同意直连(密码正确/强制),被控端跳过弹框直接进被控态;false=弹框等用户同意。
        #[serde(default)]
        auto_accept: bool,
    },
```

- [ ] **Step 2:写 decide_connect 失败测试**

`src/server/src/handlers.rs` 末尾追加:

```rust
#[cfg(test)]
mod tests {
    use super::{decide_connect, ConnectDecision};
    use protocol::Mode;

    #[test]
    fn 模式b_有密码且正确_免同意() {
        assert_eq!(decide_connect(Mode::B, true, true, false), ConnectDecision::AutoAccept);
    }
    #[test]
    fn 模式b_有密码但错_拒绝() {
        assert_eq!(decide_connect(Mode::B, true, false, false), ConnectDecision::RejectBadPassword);
    }
    #[test]
    fn 模式b_无密码_需同意() {
        assert_eq!(decide_connect(Mode::B, false, false, false), ConnectDecision::Consent);
    }
    #[test]
    fn 模式a_强制_免同意() {
        assert_eq!(decide_connect(Mode::A, false, false, true), ConnectDecision::AutoAccept);
    }
    #[test]
    fn 模式a_非强制_需同意() {
        assert_eq!(decide_connect(Mode::A, false, false, false), ConnectDecision::Consent);
    }
    #[test]
    fn 模式a_强制忽略密码态() {
        // 模式 A 不看密码,force 决定一切
        assert_eq!(decide_connect(Mode::A, true, false, true), ConnectDecision::AutoAccept);
    }
}
```

- [ ] **Step 3:运行确认失败**

Run: `cargo test -p server decide_connect`
Expected: 编译失败 `cannot find function decide_connect`(未定义)

- [ ] **Step 4:实现 decide_connect + 重写 handle_connect_request**

`src/server/src/handlers.rs`,在 `handle_connect_request` 之前加纯函数:

```rust
/// 连接决策结果(纯逻辑,便于单测)。
#[derive(Debug, PartialEq, Eq)]
pub enum ConnectDecision {
    /// 免同意直连(密码正确 / 管理员强制)。
    AutoAccept,
    /// 需被控端弹框同意。
    Consent,
    /// 模式 B 密码错误,拒绝。
    RejectBadPassword,
}

/// 纯决策:`password_ok` 由调用方对模式 B 有密码场景预先 `check_password` 求值。
pub fn decide_connect(mode: Mode, has_password: bool, password_ok: bool, force: bool) -> ConnectDecision {
    match mode {
        Mode::B => {
            if has_password {
                if password_ok { ConnectDecision::AutoAccept } else { ConnectDecision::RejectBadPassword }
            } else {
                ConnectDecision::Consent
            }
        }
        Mode::A => {
            if force { ConnectDecision::AutoAccept } else { ConnectDecision::Consent }
        }
    }
}
```

把 `handle_connect_request`(`handlers.rs:30-102`)整体替换为:

```rust
/// ConnectRequest A/B 鉴权路由:
/// - 自连/模式A越权:Reject;
/// - 模式 B 密码错:Reject + AuthFail 审计;
/// - 免同意(B 密码对 / A force):建 Active 会话 + 回 ConnectAck + 发 IncomingControl{auto_accept:true};
/// - 需同意(B 无密码 / A 非 force):建会话 + 发 IncomingControl{auto_accept:false}(等被控端 AuthResult)。
pub async fn handle_connect_request(
    hub: &Hub,
    from_id: &str,
    mode: &Mode,
    target: &str,
    password: Option<&str>,
    force: bool,
    now: i64,
) {
    if from_id == target {
        let session_id = Uuid::new_v4().to_string();
        hub.audit
            .log(&session_id, from_id, AuditType::Reject, SELF_REMOTE_REJECT_REASON)
            .await;
        send_reject(hub, from_id, session_id, SELF_REMOTE_REJECT_REASON, now);
        return;
    }

    // 模式 A 越权闸:仅已认证 admin 可发起(同时拦截非 admin 的 force,满足强制远程仅 admin)。
    if *mode == Mode::A && !from_id.starts_with("admin-") {
        tracing::warn!("拒绝非 admin 的模式A远控发起: from={from_id}");
        return;
    }

    // 模式 B 有密码时预先校验,供 decide_connect 决策。
    let has_pw = password.map(|p| !p.is_empty()).unwrap_or(false);
    let pw_ok = *mode == Mode::B && has_pw && hub.reg.check_password(target, password.unwrap_or(""));

    match decide_connect(*mode, has_pw, pw_ok, force) {
        ConnectDecision::RejectBadPassword => {
            let session_id = Uuid::new_v4().to_string();
            hub.audit
                .log(&session_id, from_id, AuditType::AuthFail, "密码错误")
                .await;
            send_reject(hub, from_id, session_id, "密码错误", now);
            return;
        }
        ConnectDecision::AutoAccept => {
            let session_id = Uuid::new_v4().to_string();
            let session = Session {
                id: session_id.clone(),
                mode: *mode,
                from_id: from_id.to_string(),
                to_id: target.to_string(),
                start_at: now,
                end_at: None,
                status: SessionStatus::Active,
            };
            hub.audit.insert_session(&session).await;
            hub.sessions.insert(session);

            // 免同意:立即回主控 ConnectAck + 落 Connect 审计(标记发起方与免同意来源)。
            let ack = Envelope {
                from: "server".into(),
                to: Some(from_id.to_string()),
                ts: now,
                payload: Message::ConnectAck { session_id: session_id.clone() },
            };
            if let Ok(json) = serde_json::to_string(&ack) {
                hub.send_to(from_id, &json);
            }
            let how = if force { "强制远程(免同意)" } else { "密码连接(免同意)" };
            hub.audit
                .log(&session_id, from_id, AuditType::Connect, how)
                .await;

            send_incoming(hub, target, session_id, from_id, *mode, true, now);
        }
        ConnectDecision::Consent => {
            let session_id = Uuid::new_v4().to_string();
            let session = Session {
                id: session_id.clone(),
                mode: *mode,
                from_id: from_id.to_string(),
                to_id: target.to_string(),
                start_at: now,
                end_at: None,
                status: SessionStatus::Active,
            };
            hub.audit.insert_session(&session).await;
            hub.sessions.insert(session);

            send_incoming(hub, target, session_id, from_id, *mode, false, now);
        }
    }
}

/// 发 IncomingControl 给被控端(携带 server 生成的 session_id 与 auto_accept 标记)。
fn send_incoming(hub: &Hub, target: &str, session_id: String, from_id: &str, mode: Mode, auto_accept: bool, now: i64) {
    let incoming = Envelope {
        from: "server".into(),
        to: Some(target.to_string()),
        ts: now,
        payload: Message::IncomingControl {
            session_id,
            from: from_id.to_string(),
            mode,
            auto_accept,
        },
    };
    if let Ok(json) = serde_json::to_string(&incoming) {
        hub.send_to(target, &json);
    }
}
```

`src/server/src/hub.rs` 的 ConnectRequest 分发(`hub.rs:137-151`)改为透传 force:

```rust
            Message::ConnectRequest {
                mode,
                target,
                password,
                force,
            } => {
                handlers::handle_connect_request(
                    self,
                    &env.from,
                    mode,
                    target,
                    password.as_deref(),
                    *force,
                    now,
                )
                .await;
            }
```

- [ ] **Step 5:修复因 ConnectRequest 加字段而破裂的 client 字面量(保编译)**

`ConnectRequest` 新增 `force` 是穷尽字段,所有 struct 字面量必须补 `force`,否则 client 编译失败:

`src/client/src/net/dispatch.rs` 的 `FromUi::StartRemote` 分支(`dispatch.rs:345-357`)暂补 `force: false`(密码归一在 Task A3 再改):

```rust
            payload: Message::ConnectRequest {
                mode: protocol::Mode::B,
                target: target_id,
                password: Some(password),
                force: false,
            },
```

`src/client/src/net/mod.rs` 测试 `上行_发起远控_序列化为_connect_request_mode_b`(`mod.rs:230-235`)的字面量补 `force`:

```rust
            payload: Message::ConnectRequest {
                mode: protocol::Mode::B,
                target: "ep-b".into(),
                password: Some("123456".into()),
                force: false,
            },
```

> `IncomingControl` 下行解构(`dispatch.rs:70`)用 `..` 吸收新字段,无需改;`hub.rs:271` 忽略臂 `IncomingControl { .. }` 同理。

- [ ] **Step 6:运行测试通过 + 全工作区编译**

Run: `cargo test -p server decide_connect && cargo build`
Expected: 6 测试 PASS;整个 workspace 编译通过

- [ ] **Step 7:提交**

```bash
git add src/protocol/src/lib.rs src/server/src/handlers.rs src/server/src/hub.rs src/client/src/net/dispatch.rs src/client/src/net/mod.rs
git commit -m "feat(server): 统一授权模型 decide_connect — 密码对/强制免同意"
```

---

### Task A2:被控端 auto_accept 直接进被控态

**Files:**
- Modify: `src/client/src/net/dispatch.rs:70-77`(IncomingControl handler)

- [ ] **Step 1:改写 IncomingControl 下行处理**

`src/client/src/net/dispatch.rs`,把 `Message::IncomingControl` 分支(`dispatch.rs:70-77`)替换为下面代码(剪贴板启动行留到 Task B4 接入,本 Task 不写,否则 `CLIPBOARD_TX` 未定义编译失败):

```rust
        // 被控端收 server 转发的来控通知。
        // auto_accept=true:免同意(密码对/强制)→ 不弹框,直接进被控态(复用同意副作用);
        // auto_accept=false:弹授权框,等用户同意。
        Message::IncomingControl {
            session_id,
            from,
            auto_accept,
            ..
        } => {
            if auto_accept {
                session.lock().await.controlled = Some(session_id.clone());
                CAPTURE_CTRL.send(CaptureCtrl::Start {
                    session_id: session_id.clone(),
                });
                let _ = to_ui.send(ToUi::BeingControlled { peer_name: from });
            } else {
                let _ = to_ui.send(ToUi::ControlRequest {
                    requester: from,
                    session_id,
                });
            }
        }
```

- [ ] **Step 2:编译 client**

Run: `cargo build -p client`
Expected: 编译通过(`IncomingControl` 新增 `auto_accept` 字段已被解构)

- [ ] **Step 3:确认 BeingControlled 设置被控横幅**

Read `src/client/src/ui_glue.rs` 中 `net::ToUi::BeingControlled` 分支(约 `ui_glue.rs:275-285`),确认其 `set_auth_pending(false)` 且 `set_being_controlled(true)`。若未设 `being_controlled`,补:

```rust
                        ui.set_peer_name(peer_name.into());
                        ui.set_being_controlled(true);
```

- [ ] **Step 4:提交**

```bash
git add src/client/src/net/dispatch.rs src/client/src/ui_glue.rs
git commit -m "feat(client): 被控端 auto_accept 免同意直接进被控态"
```

---

### Task A3:客户端伙伴密码选填

**Files:**
- Modify: `src/client/ui/app.slint:516`、`src/client/ui/app.slint:914-919`
- Modify: `src/client/src/net/dispatch.rs:345-357`(StartRemote → ConnectRequest)
- Test: `src/client/src/net/dispatch.rs`(新增 opt_password 测试)

- [ ] **Step 1:写 opt_password 失败测试**

`src/client/src/net/dispatch.rs` 末尾的测试模块内(若无则新建 `#[cfg(test)] mod tests`)加:

```rust
#[cfg(test)]
mod uplink_tests {
    use super::opt_password;

    #[test]
    fn 空密码映射为_none() {
        assert_eq!(opt_password(String::new()), None);
    }
    #[test]
    fn 非空密码映射为_some() {
        assert_eq!(opt_password("123456".into()), Some("123456".into()));
    }
}
```

- [ ] **Step 2:运行确认失败**

Run: `cargo test -p client opt_password`
Expected: 编译失败 `cannot find function opt_password`

- [ ] **Step 3:实现 opt_password + 改 StartRemote 映射**

`src/client/src/net/dispatch.rs`,在 `handle_uplink` 之前加:

```rust
/// 伙伴密码归一:空串视为「未填」→ None(选填语义,server 据此走同意流程)。
pub(super) fn opt_password(p: String) -> Option<String> {
    if p.is_empty() { None } else { Some(p) }
}
```

把 `FromUi::StartRemote` 分支(`dispatch.rs:345-357`)改为:

```rust
        FromUi::StartRemote {
            target_id,
            password,
        } => Envelope {
            from: self_id.to_string(),
            to: Some(target_id.clone()),
            ts: now(),
            payload: Message::ConnectRequest {
                mode: protocol::Mode::B,
                target: target_id,
                password: opt_password(password),
                force: false,
            },
        },
```

- [ ] **Step 4:Slint 去掉密码必填**

`src/client/ui/app.slint:516`:

```slint
                                    enabled: root.target_id != "";
```

`src/client/ui/app.slint:914-919` 的 `try-start`:

```slint
    function try-start() {
        if (root.target_id != "") {
            root.connecting = true;
            root.connect_b();
        }
    }
```

- [ ] **Step 5:运行测试 + 编译**

Run: `cargo test -p client opt_password && cargo build -p client`
Expected: 2 测试 PASS;client 编译通过

- [ ] **Step 6:提交**

```bash
git add src/client/src/net/dispatch.rs src/client/ui/app.slint
git commit -m "feat(client): 伙伴密码选填 — 空密码走同意流程"
```

---

### Task A4:ts-rs 重导出 + admin-web 申请/强制远程

**Files:**
- Regen: `cargo test -p protocol` → `src/admin-web/src/lib/types/Message.ts` 等
- Modify: `src/admin-web/src/store.ts:80,266-280`
- Modify: `src/admin-web/src/components/assets/terminal-assets.tsx`(导入/handleRemote/操作列/确认弹窗)
- Modify: `src/admin-web/src/components/assets/terminal-detail-sheet.tsx:146`

- [ ] **Step 1:重新导出 ts 类型**

Run: `cargo test -p protocol`
Expected: PASS;`git diff src/admin-web/src/lib/types/Message.ts` 显示 `connect_request` 增 `force: boolean`、`incoming_control` 增 `auto_accept: boolean`

- [ ] **Step 2:store.ts 加 force 参数**

`src/admin-web/src/store.ts:80` 类型签名:

```ts
  startRemote: (mode: "a" | "b", target: string, password: string | null, name?: string, force?: boolean) => void;
```

`src/admin-web/src/store.ts:266-280` 实现:

```ts
  startRemote(mode, target, password, name, force = false) {
    set({
      remotePhase: "connecting",
      remoteTarget: name ?? target,
      remoteRejectReason: null,
      remoteFrame: null,
      remoteNotice: null,
    });
    get().sendEnvelope({
      type: "connect_request",
      mode,
      target,
      password,
      force,
    });
  },
```

- [ ] **Step 3:terminal-assets.tsx — 导入 + 状态 + 处理函数**

`src/admin-web/src/components/assets/terminal-assets.tsx:3` 导入加 `Zap` 图标:

```tsx
import { Monitor, MoreHorizontal, RefreshCw, Search, Terminal as TerminalIcon, Trash2, Zap } from "lucide-react";
```

文件顶部导入区加 Dialog(参照 `src/admin-web/src/components/ui/dialog.tsx` 实际导出名核对):

```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
```

组件内状态(`terminal-assets.tsx:63` 附近,与 `selectedIds` 并列)加:

```tsx
  const [forceTarget, setForceTarget] = useState<{ id: string; name: string } | null>(null);
```

把 `handleRemote`(`terminal-assets.tsx:90-93`)改为带 force,并加强制确认处理:

```tsx
  // 申请远程(模式 A,需被控同意):目标取终端 id,跳 /remote 进入会话视图。
  function handleRemote(id: string, name?: string) {
    startRemote("a", id, null, name, false);
    navigate("/remote");
  }

  // 强制远程(免被控同意):二次确认后发起。
  function handleForceRemote() {
    if (!forceTarget) return;
    startRemote("a", forceTarget.id, null, forceTarget.name, true);
    setForceTarget(null);
    navigate("/remote");
  }
```

- [ ] **Step 4:terminal-assets.tsx — 操作列双按钮**

把操作列「远程控制」按钮(`terminal-assets.tsx:258-266`)替换为「申请远程」+「强制远程」:

```tsx
                    <Button
                      size="sm"
                      disabled={t.status === "offline"}
                      className="h-8 gap-1.5"
                      onClick={() => handleRemote(t.id, t.user)}
                    >
                      <TerminalIcon className="size-3.5" />
                      申请远程
                    </Button>
                    <Button
                      size="sm"
                      variant="destructive"
                      disabled={t.status === "offline"}
                      className="h-8 gap-1.5"
                      onClick={() => setForceTarget({ id: t.id, name: t.user })}
                    >
                      <Zap className="size-3.5" />
                      强制远程
                    </Button>
```

- [ ] **Step 5:terminal-assets.tsx — 二次确认弹窗**

在组件 return 的最外层容器内(与表格并列,只渲染一次,如紧邻 `<TerminalDetailSheet .../>` 处)加:

```tsx
      <Dialog open={forceTarget !== null} onOpenChange={(o) => !o && setForceTarget(null)}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>确认强制控制</DialogTitle>
            <DialogDescription>
              将强制远程控制 {forceTarget?.name} 的终端,对方不会收到同意请求、无法拒绝。请确认已获授权。
            </DialogDescription>
          </DialogHeader>
          <DialogFooter>
            <Button variant="outline" onClick={() => setForceTarget(null)}>
              取消
            </Button>
            <Button variant="destructive" onClick={handleForceRemote}>
              确认强制控制
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
```

- [ ] **Step 6:detail-sheet 文案同步**

`src/admin-web/src/components/assets/terminal-detail-sheet.tsx:146`,把「远程控制」改为「申请远程」:

```tsx
                申请远程
```

- [ ] **Step 7:类型检查 + 构建**

Run: `cd src/admin-web && npx tsc --noEmit && npm run build`
Expected: 无类型错误;构建成功

- [ ] **Step 8:提交**

```bash
git add src/admin-web/src/lib/types src/admin-web/src/store.ts src/admin-web/src/components/assets/terminal-assets.tsx src/admin-web/src/components/assets/terminal-detail-sheet.tsx
git commit -m "feat(admin-web): 申请远程/强制远程双按钮 + 二次确认"
```

---

## Phase B:剪贴板双向自动同步(功能 1)

### Task B1:协议 ClipboardSync + 路由 + 防回环纯函数

**Files:**
- Modify: `src/protocol/src/lib.rs`(Message 加 ClipboardSync)
- Modify: `src/server/src/hub.rs:193-197`(路由臂)
- Modify: `src/client/src/net/dispatch.rs`(穷尽 match 加临时忽略臂)
- Test: `src/client/src/workers.rs`(should_push_clipboard 测试)

- [ ] **Step 1:协议加 ClipboardSync 变体**

`src/protocol/src/lib.rs`,在 `RemoteNotice`(`lib.rs:220-223`)之后加:

```rust
    /// 会话内双向纯文本剪贴板同步(主控↔被控)。按 session 对端路由(同 RemoteNotice)。
    ClipboardSync {
        session_id: String,
        text: String,
    },
```

- [ ] **Step 2:server 路由臂**

`src/server/src/hub.rs:193-197`,把合并臂加上 ClipboardSync:

```rust
            Message::Frame { session_id, .. }
            | Message::RemoteNotice { session_id, .. }
            | Message::SetQuality { session_id, .. }
            | Message::ClipboardSync { session_id, .. } => {
                self.route_to_peer(session_id, &env);
            }
```

- [ ] **Step 3:client 下行临时忽略臂(保编译)**

`src/client/src/net/dispatch.rs` 的 `handle_downlink` match 内,加临时臂(Task B4 替换为真实写入):

```rust
        // 剪贴板同步(Task B4 接入 CLIPBOARD_TX 写本地)
        Message::ClipboardSync { .. } => {}
```

- [ ] **Step 4:写 should_push_clipboard 失败测试**

`src/client/src/workers.rs` 末尾加:

```rust
#[cfg(test)]
mod clipboard_tests {
    use super::should_push_clipboard;

    #[test]
    fn 非空且变化_应推送() {
        assert!(should_push_clipboard("hello", ""));
        assert!(should_push_clipboard("world", "hello"));
    }
    #[test]
    fn 空文本_不推送() {
        assert!(!should_push_clipboard("", "hello"));
    }
    #[test]
    fn 未变化_不推送() {
        assert!(!should_push_clipboard("same", "same"));
    }
}
```

- [ ] **Step 5:运行确认失败**

Run: `cargo test -p client should_push_clipboard`
Expected: 编译失败 `cannot find function should_push_clipboard`

- [ ] **Step 6:实现 should_push_clipboard**

`src/client/src/workers.rs` 顶部(use 之后)加:

```rust
/// 是否应把本地剪贴板变化推给对端:非空且与上次同步值不同(防回环核心)。
pub fn should_push_clipboard(current: &str, last_synced: &str) -> bool {
    !current.is_empty() && current != last_synced
}
```

- [ ] **Step 7:运行测试 + 全编译**

Run: `cargo test -p client should_push_clipboard && cargo build -p server -p client`
Expected: 3 测试 PASS;server/client 编译通过

- [ ] **Step 8:提交**

```bash
git add src/protocol/src/lib.rs src/server/src/hub.rs src/client/src/net/dispatch.rs src/client/src/workers.rs
git commit -m "feat: 协议 ClipboardSync + server 路由 + 防回环纯函数"
```

---

### Task B2:net 层 ClipboardMsg 桥 + FromUi 变体

**Files:**
- Modify: `src/client/src/net/mod.rs`(FromUi + ClipboardBridge/ClipboardMsg)

- [ ] **Step 1:FromUi 加 ClipboardSync**

`src/client/src/net/mod.rs` 的 `FromUi` 枚举(`mod.rs:97-99` 附近,`RefreshPassword` 前)加:

```rust
    /// 本端剪贴板变化 → 推给对端(会话内双向同步)。
    ClipboardSync { session_id: String, text: String },
```

- [ ] **Step 2:加 ClipboardMsg + ClipboardBridge**

`src/client/src/net/mod.rs`,在 `CAPTURE_CTRL`(`mod.rs:154`)之后加桥声明:

```rust
/// 剪贴板控制/写入旁路:net 收下行/会话启停 → 交 clipboard worker 线程(独占 arboard)。
pub static CLIPBOARD_TX: ClipboardBridge = ClipboardBridge(OnceLock::new());
```

在 `CaptureCtrlBridge` 实现(`mod.rs:199`)之后加:

```rust
/// 剪贴板 worker 控制消息:会话启停 + 对端写入。
#[derive(Debug, Clone)]
pub enum ClipboardMsg {
    /// 会话激活:开始轮询本地剪贴板,变化推 `session_id` 对端。
    Start { session_id: String },
    /// 会话结束:停止轮询并清空 last_synced。
    Stop,
    /// 收到对端剪贴板文本:写本地 + 更新 last_synced(防回环)。
    Incoming { text: String },
}

pub struct ClipboardBridge(OnceLock<mpsc::UnboundedSender<ClipboardMsg>>);
impl ClipboardBridge {
    pub fn init(&self, tx: mpsc::UnboundedSender<ClipboardMsg>) {
        let _ = self.0.set(tx);
    }
    // 私有 fn(与 CaptureCtrlBridge::send 一致):私有项对子模块 net::dispatch 可见。
    fn send(&self, m: ClipboardMsg) {
        if let Some(tx) = self.0.get() {
            let _ = tx.send(m);
        }
    }
}
```

- [ ] **Step 3:补 handle_uplink 出站映射(否则 FromUi 加变体后 match 不穷尽)**

`src/client/src/net/dispatch.rs` 的 `handle_uplink`,在 `FromUi::SetQuality` 分支(`dispatch.rs:413-418`)之后加:

```rust
        FromUi::ClipboardSync { session_id, text } => Envelope {
            from: self_id.to_string(),
            to: None, // server 按 session_id 路由给对端
            ts: now(),
            payload: Message::ClipboardSync { session_id, text },
        },
```

- [ ] **Step 4:编译**

Run: `cargo build -p client`
Expected: 编译通过(`CLIPBOARD_TX`/`ClipboardMsg` 暂未被消费,可能有 dead_code 告警,Task B3/B4 接入)

- [ ] **Step 5:提交**

```bash
git add src/client/src/net/mod.rs src/client/src/net/dispatch.rs
git commit -m "feat(client): 剪贴板旁路桥 + FromUi::ClipboardSync 出站映射"
```

---

### Task B3:consume_clipboard worker + main 挂载

**Files:**
- Modify: `src/client/src/workers.rs`(consume_clipboard)
- Modify: `src/client/src/main.rs:85-86,110-112`(通道 + 挂载)

- [ ] **Step 1:实现 consume_clipboard**

`src/client/src/workers.rs`,`use` 区确认含 `use std::sync::Arc;`(已有),并在文件末尾(测试模块前)加:

```rust
/// 剪贴板双向同步 worker。
///
/// arboard 句柄非 Send 且 Linux/X11 下需常驻持有,故独占一个 std::thread;`last_synced` 线程私有,
/// 收(写本地)与发(poll)都更新它,值相同即不发 —— 单线程内无竞态、无 A→B→A 回弹。
/// 控制信号(Start/Stop/Incoming)经 tokio mpsc 转进线程的 std mpsc。
pub async fn consume_clipboard(
    mut ctrl_rx: tokio::sync::mpsc::UnboundedReceiver<net::ClipboardMsg>,
    from_ui_tx: tokio::sync::mpsc::UnboundedSender<net::FromUi>,
) {
    let active: Arc<std::sync::Mutex<Option<String>>> = Arc::new(std::sync::Mutex::new(None));
    let (blk_tx, blk_rx) = std::sync::mpsc::channel::<String>(); // 对端写入文本

    // 剪贴板线程:独占 arboard,poll 本地 + 写对端。
    {
        let active = active.clone();
        std::thread::spawn(move || {
            if crate::capture::is_wayland_session() {
                tracing::warn!("Wayland 会话:剪贴板同步禁用(arboard 在 Wayland 不可靠)");
                return;
            }
            let mut clip = match arboard::Clipboard::new() {
                Ok(c) => c,
                Err(e) => {
                    tracing::warn!("剪贴板不可用:{e},同步禁用");
                    return;
                }
            };
            let mut last_synced = String::new();
            let mut prev_active: Option<String> = None;
            loop {
                std::thread::sleep(std::time::Duration::from_millis(500));
                // 先处理对端写入(可能多条,取最后一条即可)。
                while let Ok(text) = blk_rx.try_recv() {
                    if clip.set_text(text.clone()).is_ok() {
                        last_synced = text;
                    }
                }
                let cur_active = active.lock().unwrap().clone();
                // 会话切换:新会话用当前剪贴板做基线(不推送会话前的旧内容);结束清空。
                if cur_active != prev_active {
                    last_synced = clip.get_text().unwrap_or_default();
                    prev_active = cur_active.clone();
                }
                let sid = match cur_active {
                    Some(s) => s,
                    None => continue,
                };
                let cur = match clip.get_text() {
                    Ok(t) => t,
                    Err(_) => continue, // 空剪贴板/非文本:跳过
                };
                if cur.len() > 256 * 1024 {
                    tracing::debug!("剪贴板文本过大({} 字节),跳过同步", cur.len());
                    last_synced = cur; // 记为已同步,避免反复命中
                    continue;
                }
                if should_push_clipboard(&cur, &last_synced) {
                    last_synced = cur.clone();
                    if from_ui_tx
                        .send(net::FromUi::ClipboardSync { session_id: sid, text: cur })
                        .is_err()
                    {
                        break; // net 已退出
                    }
                }
            }
        });
    }

    // 控制信号消费:更新活跃会话 / 转发对端写入。
    while let Some(m) = ctrl_rx.recv().await {
        match m {
            net::ClipboardMsg::Start { session_id } => {
                *active.lock().unwrap() = Some(session_id);
            }
            net::ClipboardMsg::Stop => {
                *active.lock().unwrap() = None;
            }
            net::ClipboardMsg::Incoming { text } => {
                let _ = blk_tx.send(text);
            }
        }
    }
}
```

> 依赖确认:`crate::capture::is_wayland_session()` 已存在(`workers.rs:118` 已调用)。

- [ ] **Step 2:main.rs 加通道 + 挂载**

`src/client/src/main.rs:85-86`(`CAPTURE_CTRL.init` 之后)加:

```rust
    let (clip_tx, clip_rx) = tokio::sync::mpsc::unbounded_channel::<net::ClipboardMsg>();
    net::CLIPBOARD_TX.init(clip_tx);
```

`src/client/src/main.rs:111-112`,把 capture 的 from_ui_tx 改为 clone,并加 clipboard worker:

```rust
    rt.spawn(workers::consume_screenshot(shot_rx, from_ui_tx.clone()));
    rt.spawn(workers::consume_capture(cap_rx, from_ui_tx.clone()));
    rt.spawn(workers::consume_clipboard(clip_rx, from_ui_tx));
```

- [ ] **Step 3:编译**

Run: `cargo build -p client`
Expected: 编译通过(出站映射已在 Task B2 加;worker 已消费 `ClipboardMsg`/产出 `FromUi::ClipboardSync`,dead_code 告警消除)

- [ ] **Step 4:提交**

```bash
git add src/client/src/workers.rs src/client/src/main.rs
git commit -m "feat(client): consume_clipboard 独占线程 worker + main 挂载"
```

---

### Task B4:接线启停点 + 下行写入

**Files:**
- Modify: `src/client/src/net/dispatch.rs`(IncomingControl auto_accept / ConnectAck / SessionEnd / Disconnect / AuthDecision / ClipboardSync 下行)

- [ ] **Step 1:导入 CLIPBOARD_TX / ClipboardMsg**

`src/client/src/net/dispatch.rs:9` 的 `use` 列表加入(与 `CAPTURE_CTRL` 同模式,后续可非限定引用):

```rust
use super::{now, CaptureCtrl, ClipboardMsg, FromUi, ToUi, CAPTURE_CTRL, CLIPBOARD_TX, INJECT_TX, SCREENSHOT_TX};
```

- [ ] **Step 2:被控 auto_accept 进被控态时启动剪贴板**

`Message::IncomingControl` auto_accept 分支(Task A2 写入)内,在 `CAPTURE_CTRL.send(...)` 之后加:

```rust
                CLIPBOARD_TX.send(ClipboardMsg::Start {
                    session_id: session_id.clone(),
                });
```

- [ ] **Step 3:被控同意(上行)启动剪贴板**

`handle_uplink` → `FromUi::AuthDecision` accept 分支(`dispatch.rs:359-367`),在 `CAPTURE_CTRL.send(CaptureCtrl::Start{...})` 之后加:

```rust
                CLIPBOARD_TX.send(ClipboardMsg::Start {
                    session_id: session_id.clone(),
                });
```

- [ ] **Step 4:主控 ConnectAck 启动剪贴板**

`Message::ConnectAck` 下行分支(`dispatch.rs:100-103`)改为:

```rust
        Message::ConnectAck { session_id } => {
            session.lock().await.controlling = Some(session_id.clone());
            CLIPBOARD_TX.send(ClipboardMsg::Start {
                session_id: session_id.clone(),
            });
            let _ = to_ui.send(ToUi::RemoteAck { session_id });
        }
```

- [ ] **Step 5:会话结束/断开停止剪贴板**

`Message::SessionEnd` 下行分支(`dispatch.rs:156-166`),在 `to_ui.send(ToUi::SessionEnded)` 之前加:

```rust
            CLIPBOARD_TX.send(ClipboardMsg::Stop);
```

`handle_uplink` → `FromUi::Disconnect` 分支(`dispatch.rs:419` 起),在 `session.lock().await.controlling = None;` 之后加:

```rust
            CLIPBOARD_TX.send(ClipboardMsg::Stop);
```

- [ ] **Step 6:下行 ClipboardSync 写本地(替换 Task B1 的临时忽略臂)**

把 Task B1 加的临时臂 `Message::ClipboardSync { .. } => {}` 替换为:

```rust
        // 被控/主控收对端剪贴板 → 校验方向后交 worker 写本地(防回环由 worker 的 last_synced 处理)。
        Message::ClipboardSync { session_id, text } => {
            let ctx = session.lock().await;
            let in_session = ctx.controlling.as_deref() == Some(session_id.as_str())
                || ctx.controlled.as_deref() == Some(session_id.as_str());
            drop(ctx);
            if in_session {
                CLIPBOARD_TX.send(ClipboardMsg::Incoming { text });
            }
        }
```

- [ ] **Step 7:编译 + 全量客户端测试**

Run: `cargo build -p client && cargo test -p client`
Expected: 编译通过;既有测试 + 新增测试全 PASS

- [ ] **Step 8:提交**

```bash
git add src/client/src/net/dispatch.rs
git commit -m "feat(client): 剪贴板会话启停接线 + 下行写本地"
```

---

## Phase C:临时密码 UI(功能 4)

### Task C1:ValueRow 掩码 + 眼睛/复制/刷新图标

**Files:**
- Modify: `src/client/ui/app.slint:194-225`(ValueRow masked)
- Modify: `src/client/ui/app.slint:296`(password_visible 属性)
- Modify: `src/client/ui/app.slint:470-486`(临时密码行)

> **图标字符约定**(spec 模块 E):刷新 `↻`(已验证,`app.slint:789` 在用)。眼睛 `◉`(隐藏态,点击显示)/ `○`(明文态,点击隐藏);复制 `⧉`。**Step 5 真机验证**:若任一字符显示为缺字形方块,退化 —— 眼睛用 `显`/`隐`,复制用 `复制`。

- [ ] **Step 1:ValueRow 加 masked**

`src/client/ui/app.slint:194-197`,`ValueRow` 属性区加 `masked`:

```slint
component ValueRow inherits Rectangle {
    in property <string> caption;
    in property <string> value;
    in property <bool> masked: false;
    in property <color> value-color: Theme.fg;
```

`src/client/ui/app.slint:212-213` 的 value Text,改 `text`:

```slint
            Text {
                text: root.masked ? "••••••" : root.value;
```

- [ ] **Step 2:AppWindow 加 password_visible 属性**

`src/client/ui/app.slint:296`(`self_password` 属性之后)加:

```slint
    in-out property <bool> password_visible: false;  // 临时密码是否明文显示(眼睛切换)
```

- [ ] **Step 3:临时密码行 — 掩码 + 三图标**

`src/client/ui/app.slint:470-486` 的临时密码 `ValueRow` 替换为:

```slint
                                ValueRow {
                                    caption: "临时密码";
                                    value: root.self_password;
                                    value-color: Theme.emerald;
                                    masked: !root.password_visible;
                                    GhostButton {
                                        label: root.password_visible ? "○" : "◉";
                                        clicked => {
                                            root.password_visible = !root.password_visible;
                                        }
                                    }
                                    GhostButton {
                                        label: "↻";
                                        clicked => {
                                            root.refresh_password();
                                        }
                                    }
                                    GhostButton {
                                        label: "⧉";
                                        clicked => {
                                            root.copy_text(root.self_password);
                                        }
                                    }
                                }
```

- [ ] **Step 4:编译**

Run: `cargo build -p client`
Expected: 编译通过

- [ ] **Step 5:真机视觉验证(关键)**

Run: `cargo run -p client -- 测试终端`(X11 桌面)
检查空闲页「临时密码」行:
1. 默认显示 `••••••`(掩码)
2. 点眼睛 `◉` → 显示 6 位明文,图标变 `○`;再点 → 回掩码
3. 点 `↻` → 密码刷新(数字变化)
4. 点 `⧉` → 剪贴板得到密码(粘贴验证)

Expected: 四项均正常,且 `◉/○/↻/⧉` 均为清晰字形(非方块)。
**若出现方块**:退化 —— 眼睛 label 改 `root.password_visible ? "隐" : "显"`,复制 label 改 `"复制"`,重新 `cargo run` 验证。

- [ ] **Step 6:提交**

```bash
git add src/client/ui/app.slint
git commit -m "feat(client): 临时密码默认隐藏 + 眼睛/复制/刷新图标"
```

---

## 集成验证(全部 Task 完成后)

- [ ] **四授权路径端到端**(两客户端 + admin-web + 本地 server):
  1. 客户端填对密码连 → 对方**不弹框**直接被控
  2. 客户端不填密码连 → 对方**弹框**,同意后被控
  3. 客户端填错密码 → 主控收「密码错误」
  4. admin-web「申请远程」→ 对方弹框;「强制远程」→ 二次确认后对方**不弹框**直接被控
- [ ] **剪贴板双向不回环**:主控复制文本 → 被控粘贴到;被控复制 → 主控粘贴到;反复复制不卡死、不回弹
- [ ] **被控免同意自动启动截屏**:auto_accept 路径下主控立即收到画面帧
- [ ] **回归**:`cargo test --workspace` 全绿;Windows 交叉编译 `cargo build -p client --target x86_64-pc-windows-gnu`;`cargo clippy --workspace -- -D warnings`

## 发版

四功能验证通过后发 **v0.1.8**(服务器 + 客户端 + admin-web + download 页),沿用既有发布流程。
